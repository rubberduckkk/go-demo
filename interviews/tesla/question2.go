package tesla

/*
Input: A []int, B []int, N int, len(A) == len(B)
Question: (A[i], B[i]) means cityA and cityB are connected by a road.
	The network rank between two cities A and B is calculated by the total number of roads connected to them.
Output: the maximum network rank within the road system provided.
*/

func calcNetworkRank(A []int, B []int, N int) int {
	return 0
}
